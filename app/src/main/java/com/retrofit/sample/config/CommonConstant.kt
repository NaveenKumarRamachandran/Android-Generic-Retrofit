package com.retrofit.sample.config

interface CommonConstant {

    companion object {

        const val RESPONSE_CODE = "responseCode"

        const val RESPONSE_MESSAGE = "responseMessage"

        const val DATA = "data"
    }
}